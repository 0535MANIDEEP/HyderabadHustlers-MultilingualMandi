{
  "installCommand": "cd frontend && npm install",
  "buildCommand": "cd frontend && NODE_OPTIONS=--openssl-legacy-provider npm run build",
  "outputDirectory": "frontend/build",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
